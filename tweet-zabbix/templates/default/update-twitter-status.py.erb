#!/usr/bin/python

import sys
import tweepy

from twitter_keys import *

auth = tweepy.OAuthHandler(CONSUMER_KEY, CONSUMER_SECRET)
auth.set_access_token(ACCESS_KEY, ACCESS_SECRET)
api = tweepy.API(auth)

mentions = "@imksoo "
tweet_len = 140 - len(mentions) - 2
api.update_status(mentions + sys.argv[2][0:tweet_len])

